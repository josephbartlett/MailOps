# Proton Rule Examples

Generate review-only Sieve previews with:

```bash
mailops rules propose "filter future messages from vendor.example to label Finance"
```

MailOps prints the structured conditions, generated Proton Sieve text, and preview. It does not apply provider rules.
