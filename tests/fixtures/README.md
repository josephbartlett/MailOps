# Fixtures

This directory is reserved for provider and mailbox fixtures used by integration tests.

For contributor-facing local data, use:

```bash
mailops demo seed
```

That command seeds a deterministic `demo_local` mailbox into the active MailOps home without requiring Proton Bridge.
